git init
npm init

# install the dependencies
npm i express mongoose

# install globaly the node tool nodemon
npm i -g nodemon

# adding header to every request
npm i cors

#install globaly the firebase-tools 
npm i -g firebase-tools